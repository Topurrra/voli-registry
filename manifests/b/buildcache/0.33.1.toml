name = "buildcache"
version = "0.33.1"
description = "An advanced compiler accelerator that caches and reuses build results to avoid unnecessary re-compilations, and thereby speeding up the build process."
homepage = "https://gitlab.com/bits-n-bites/buildcache"
license = "Zlib"
kind = "app"
extract_dir = "buildcache"
bin = ["bin/buildcache.exe"]
shortcuts = [{ target = "bin/buildcache.exe", name = "buildcache" }]

[source.x64]
url = "https://gitlab.com/bits-n-bites/buildcache/-/releases/v0.33.1/downloads/buildcache-windows.zip"
sha256 = "c138482e0f906486bd7ebfe7422d6bdc5d63d3206c7a7a17cf3aec6a9f6f9469"

[autoupdate]
checkver = { url = "https://gitlab.com/api/v4/projects/49153623/releases/permalink/latest", regex = "([\\d.]+)" }
url_template = { x64 = "https://gitlab.com/bits-n-bites/buildcache/-/releases/v$version/downloads/buildcache-windows.zip" }
