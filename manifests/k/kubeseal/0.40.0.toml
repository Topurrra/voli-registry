name = "kubeseal"
version = "0.40.0"
description = "With kubeseal you can encrypt your Kubernetes Secret into a SealedSecret, which is safe to store - even to a public repository. The SealedSecret can be decrypted only by the controller running in the target cluster and nobody else (not even the original author) is able to obtain the original Secret from the SealedSecret."
homepage = "https://github.com/bitnami/sealed-secrets"
license = "Apache-2.0"
kind = "app"
bin = ["kubeseal.exe"]

[source.x64]
url = "https://github.com/bitnami/sealed-secrets/releases/download/v0.40.0/kubeseal-0.40.0-windows-amd64.tar.gz"
sha256 = "e43f3208b41709f69eea03059fcfe5688152543184ee0d7454f0e6d709466821"

[autoupdate]
checkver = { github = "bitnami/sealed-secrets" }
url_template = { x64 = "https://github.com/bitnami/sealed-secrets/releases/download/v$version/kubeseal-$version-windows-amd64.tar.gz" }
